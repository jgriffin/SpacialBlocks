//
// Created by John Griffin on 4/4/24
//

import Spatial

extension Vector3D {
    static let one = Vector3D([1.0, 1.0, 1.0])
    static let half = Vector3D([0.5, 0.5, 0.5])
}

extension Size3D {
    static let one = Size3D([1.0, 1.0, 1.0])
    static let half = Size3D([0.5, 0.5, 0.5])
}

extension Point3D {
    static let one = Point3D([1.0, 1.0, 1.0])
    static let half = Point3D([0.5, 0.5, 0.5])
}
